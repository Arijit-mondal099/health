/**
 * Gender constants.
 * @module constants/gender
 */

/** Allowed gender values (kept in sync with the User model `gender` enum). */
export const GENDERS = Object.freeze(["male", "female", "other", "not selected"]);