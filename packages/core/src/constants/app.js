/**
 * Application-wide constants.
 * @module constants/app
 */

/** Default pagination page size used across list endpoints. */
export const DEFAULT_PAGE_SIZE = 10;

/** Maximum allowed pagination page size. */
export const MAX_PAGE_SIZE = 100;

/** ISO 4217 currency code used for monetary formatting (Razorpay/INR). */
export const CURRENCY = "INR";

/** BCP 47 locale used for Intl formatting. */
export const LOCALE = "en-IN";

/** Human-readable product name. */
export const APP_NAME = "Health";