{
    "name": "health",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "type": "module",
    "scripts": {
        "dev:web": "bun --cwd apps/web vite",
        "dev:api": "bun --cwd apps/api dev",
        "dev:admin": "bun --cwd apps/admin vite",
        "build:web": "bun --cwd apps/web run build",
        "build:admin": "bun --cwd apps/admin run build",
        "build:api": "echo \"No build required\"",
        "start:api": "bun --cwd apps/api run start",
        "format": "oxfmt . --write",
        "format:check": "oxfmt . --check",
        "lint": "oxlint . --deny-warnings",
        "lint:fix": "oxlint . --fix",
        "prepare": "husky"
    },
    "devDependencies": {
        "@commitlint/cli": "^21.2.1",
        "@commitlint/config-conventional": "^21.2.0",
        "@types/node": "^26.1.1",
        "husky": "^9.1.7",
        "oxfmt": "^0.58.0",
        "oxlint": "^1.73.0"
    },
    "engines": {
        "node": ">=22"
    }
}